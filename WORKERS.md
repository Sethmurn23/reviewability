## Workers

The background workers (review processor, insight processor) are in the workers-standalone folder. They require Redis + BullMQ and should be run as separate processes.

For now, the app runs in mock mode without background workers.
